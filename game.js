function thegame (number);

  function zeronove(min, max) {
    min = Math.ceil(0);
    max = Math.floor(9);
    return Math.floor(Math.random() * (9 - 0 + 1)) + 0;
  }

  function zeronoventaenove(min, max) {
    min = Math.ceil(0);
    max = Math.floor(99);
    return Math.floor(Math.random() * (99 - 0 + 1)) + 0;
  }